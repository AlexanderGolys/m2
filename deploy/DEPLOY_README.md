#!/bin/bash
# Use deploy_backend.sh which handles all deployments

echo "This script is no longer needed"
echo "Use: bash deploy_backend.sh instead"
echo ""
echo "Usage:"
echo "  bash deploy_backend.sh           # Auto-detect and deploy what changed"
echo "  bash deploy_backend.sh --backend # Force backend deployment only"
echo "  bash deploy_backend.sh --frontend # Force frontend deployment only"
